config {
    type: "table",
    schema: "enrich_data",
    name: "average_courses_by_year_cv",
    description: "This table shows the average of cv by year"
}
select 
ano,
AVG(candidato_vaga) AS avg_candidato_vaga
from ${ref("cv_clean")} 
GROUP BY ano